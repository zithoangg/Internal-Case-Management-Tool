{
  "name": "internal-case-management-tool",
  "version": "2.0.0",
  "private": true,
  "description": "Internal note-generator for Azure support engineers (Title / Case / Risk / SOAP).",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./app.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./app.css --watch",
    "test": "node --test tests/site.test.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  },
  "dependencies": {
    "air-datepicker": "^3.6.0"
  }
}
